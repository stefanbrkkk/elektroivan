/**
 * Custom cursor (dot + ring), fine pointers only (docs/BRIEF.md §4). Renders
 * nothing in phase 1; motion-engineer implements the follow/magnetic
 * behaviour later, gated behind `useFinePointer()` + reduced-motion checks.
 */
export function Cursor() {
  return null;
}
